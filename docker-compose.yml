services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
      - ./output:/app/output
      - ./config:/app/config
    environment:
      - OUTPUT_FILE=/app/output/results.jsonl
